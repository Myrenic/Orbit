---
persistence:
  defaultClassReplicaCount: 3
  allowVolumeExpansion: true
  reclaimPolicy: Delete
  volumeBindingMode: Immediate

preUpgradeChecker:
  jobEnabled: false
  upgradeVersionCheck: true

metrics:
  serviceMonitor:
    enabled: true
    additionalLabels:
      release: kube-prometheus-stack

defaultBackupStore:
  backupTarget: azblob://longhorn@velero76b1f66a064d/
  backupTargetCredentialSecret: azure-backup-credentials
  pollInterval: "300"
